# marketplace-expense-logger

Deploy repo for the **expense-logger** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs expense-logger --push`.

Coolify: base_directory `/`, dockerfile `/apps/expense-logger/Dockerfile`.
